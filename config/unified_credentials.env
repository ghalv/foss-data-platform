# FOSS Data Platform - Unified Credentials
# All services use consistent admin credentials for logical consistency

# Admin User (consistent across all services)
ADMIN_USER=admin
ADMIN_PASSWORD=admin123

# Database Credentials
DB_USER=dagster
DB_PASSWORD=dagster123
DB_NAME=dagster

# MinIO Credentials (S3-compatible)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=admin123

# JupyterLab (no authentication required)
JUPYTER_TOKEN=
JUPYTER_PASSWORD=

# Service URLs (internal container communication)
TRINO_URL=http://trino-coordinator:8080
POSTGRES_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
MINIO_URL=http://minio:9000
REDIS_URL=redis://redis:6379
